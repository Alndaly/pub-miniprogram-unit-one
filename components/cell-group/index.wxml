<view
  wx:if="{{ title }}"
  class="van-cell-group__title"
>
  {{ title }}
</view>
<view class="van-cell-group {{ border ? 'van-hairline--top-bottom' : '' }}" style="{{customStyle}}">
  <slot />
</view>
